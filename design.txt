def search_lost(category: str, location: str,
    start_period: str, end_period: str, region: str,
    name: str, control_number: str): -> List[str, str, str, str]

    # 분류명 선택가능 리스트 존재
    # 기간은 yyyymmdd 형태만 입력가능
    # 분실지역 선택가능 리스트 존재
    # 기간은 null 입력 불가능, 나머지는 null 입력 가능

def connect(self):
    # 작업 페이지로 접근하는 함수

def enter_condition(self):
    # 내부적인 조건들 페이지에 입력

def 

